**Specialized dictionaries**
- In this section we are going to look at other specialized types of dictionaries that Python standard library provides
```aiignore
defaultdict     automatic default values for "missing" keys
OrderedDict     guaranteeed key ordering (based on insertion order), plus some extras
Counter         specialized tools for dealing with counters
ChainMap        effcient way of "combining" multiple dictionaries
UserDict        alternative to subclassing dict for creating custom dictioary types
```

**Missing keys**

